{
  "name": "@blockr/blockr-p2p-lib",
  "version": "1.0.0",
  "description": "Blockr P2P node library",
  "main": "dist/peer.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build-watch": "tsc -w -p tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "tslint --project tsconfig.json",
    "dev": "npm run build && npm run lint"
  },
  "author": "Blockr (Hugo van de Vliert, Ronald van den Broek)",
  "license": "MIT",
  "dependencies": {
    "@blockr/blockr-logger": "^1.0.0-2",
    "@types/node": "^11.13.7",
    "socket.io": "^2.2.0",
    "socket.io-client": "^2.2.0",
    "tslint": "^5.16.0",
    "tsutils": "^3.10.0",
    "typescript": "^3.4.3"
  },
  "devDependencies": {
    "@types/socket.io": "^2.1.2",
    "@types/socket.io-client": "^1.4.32"
  }
}

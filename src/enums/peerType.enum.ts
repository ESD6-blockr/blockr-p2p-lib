
/**
 * Peer type
 */
export enum PeerType {
    VALIDATOR = "validator",
    SMART_CONTRACT_ENGINE = "smart_contract_engine",
    WALLET = "wallet",
    IPFS = "ipfs",
    INITIAL_PEER = "initial_peer",
}

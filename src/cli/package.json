{
  "name": "p2p-cli",
  "version": "1.0.0",
  "description": "Command line interface to test P2P communication",
  "main": "index.js",
  "dependencies": {
    "chalk": "^2.4.2",
    "figlet": "^1.2.1",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "@types/node": "^12.0.0",
    "ts-node": "^8.1.0",
    "typescript": "^3.4.5"
  },
  "scripts": {
    "dev": "npm run build && node dist/index.js",
    "build": "tsc -p tsconfig.json",
    "build-watch": "tsc -w -p tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Blockr (Juul Damen)",
  "license": "MIT"
}
